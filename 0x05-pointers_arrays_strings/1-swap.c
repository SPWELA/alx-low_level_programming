#include "main.h"

/**
 * void spw - spwas int
 * @c : input
 * Return (0)
 */
void swap_int(int *a, int *b);
int main(void)
{
	char ch = 'a=98';
	char ch = 'b=42';
	for (ch = 'a=98'; ch = 'b=42'; ch++)
		putchar(ch);
	return (0);
	swap _int (&a, &b);
	printf("a=%d, b=%d\n" , a, b);
	return (0);
}
